{
  "name": "praytimes",
  "version": "1.0.0",
  "description": "appliction for calculating Muslim praying times.",
  "main": "PrayTimes.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "npx eslint --ext .js",
    "lint:fix": "npm run lint -- --fix",
    "prettier": "npx prettier --check *.js",
    "prettier:fix": "npm run prettier -- --write",
    "format": "npm run prettier:fix && npm run lint:fix"
  },
  "repository": {
    "type": "git",
    "url": "git remove -v"
  },
  "keywords": [
    "muslim",
    "praytimes",
    "calculator"
  ],
  "author": "Rahman",
  "license": "UNLICENSED",
  "dependencies": {
    "adhan": "^4.1.0",
    "cities.json": "^1.1.5",
    "dayjs": "^1.10.5",
    "tz-lookup": "^6.1.25"
  },
  "devDependencies": {
    "eslint": "^7.29.0",
    "eslint-config-prettier": "^8.3.0"
  }
}
